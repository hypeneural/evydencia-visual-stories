---
import BaseLayout from "./BaseLayout.astro";
import WhatsAppCTA from "@/components/ui/WhatsAppCTA.astro";
import { BUSINESS_DATA } from "@/data/business";
import { getCanonicalUrl } from "@/lib/seo/canonical";

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  author: string;
  featuredImage: string;
  tags?: string[];
  readingTime?: string;
}

const {
  title,
  description,
  pubDate,
  author,
  featuredImage,
  tags = [],
  readingTime = "5 min"
} = Astro.props;

const canonicalUrl = getCanonicalUrl(Astro.url.pathname);
const formattedDate = new Intl.DateTimeFormat("pt-BR", {
  day: "numeric",
  month: "long",
  year: "numeric"
}).format(pubDate);

const articleSchema = {
  "@context": "https://schema.org",
  "@type": "BlogPosting",
  "headline": title,
  "description": description,
  "image": featuredImage,
  "datePublished": pubDate.toISOString(),
  "author": {
    "@type": "Person",
    "name": author
  },
  "publisher": {
    "@id": `${BUSINESS_DATA.url}/#business`
  },
  "mainEntityOfPage": canonicalUrl
};
---

<BaseLayout
  title={title}
  description={description}
  image={featuredImage}
  type="article"
  schema={articleSchema}
>
  <article class="py-12 bg-white">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      
      <!-- Cabeçalho do Artigo -->
      <header class="text-center space-y-4 mb-8">
        <div class="flex flex-wrap items-center justify-center gap-2 text-xs font-semibold text-brand-gold-dark uppercase tracking-wider">
          {tags.map((tag) => (
            <span class="bg-brand-beige px-3 py-1 rounded-full">{tag}</span>
          ))}
          <span class="text-gray-400">•</span>
          <span class="text-gray-500 font-normal">{readingTime}</span>
        </div>

        <h1 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-gray-900 font-serif-brand leading-tight">
          {title}
        </h1>

        <div class="flex items-center justify-center gap-3 text-sm text-gray-500 pt-2">
          <span>Por <strong>{author}</strong></span>
          <span>•</span>
          <time datetime={pubDate.toISOString()}>{formattedDate}</time>
        </div>
      </header>

      <!-- Imagem de Destaque -->
      <div class="rounded-2xl overflow-hidden shadow-md mb-10 aspect-[16/9] bg-gray-100">
        <img
          src={featuredImage}
          alt={title}
          width="900"
          height="506"
          class="w-full h-full object-cover"
          loading="eager"
        />
      </div>

      <!-- Corpo do Post -->
      <div class="prose prose-lg max-w-none text-gray-700 leading-relaxed">
        <slot />
      </div>

      <!-- Banner de Conversão -->
      <div class="mt-16">
        <WhatsAppCTA variant="banner" />
      </div>

    </div>
  </article>
</BaseLayout>
