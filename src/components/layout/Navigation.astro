---
import { MAIN_NAVIGATION } from "@/data/navigation";

interface Props {
  currentPath?: string | undefined;
  class?: string | undefined;
}

const { currentPath = "/", class: className = "" } = Astro.props;
---

<nav class={`flex items-center gap-1 xl:gap-2 ${className}`} aria-label="Navegação Principal">
  {MAIN_NAVIGATION.map((link) => {
    const isActive = currentPath === link.href || (link.href !== "/" && currentPath.startsWith(link.href));
    return (
      <a
        href={link.href}
        class={`px-3 py-2 text-sm font-medium rounded-md transition-colors ${
          isActive 
            ? "text-brand-gold font-semibold" 
            : "text-gray-700 hover:text-brand-gold hover:bg-brand-beige"
        }`}
        aria-current={isActive ? "page" : undefined}
      >
        {link.label}
      </a>
    );
  })}
</nav>
