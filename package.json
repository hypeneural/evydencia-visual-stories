{
  "name": "evydencia",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "check": "astro check --minimumFailingSeverity warning",
    "build:astro": "astro build",
    "seo:images": "node scripts/generate-image-sitemap.mjs",
    "seo:validate": "node scripts/validate-seo.mjs && node scripts/validate-links.mjs && node scripts/validate-images.mjs",
    "build": "npm run check && npm run build:astro && npm run seo:images && npm run seo:validate",
    "preview": "astro preview",
    "test": "playwright test",
    "lhci": "lhci autorun",
    "indexnow": "node scripts/submit-indexnow.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^5.2.5",
    "schema-dts": "^2.0.0",
    "sharp": "^0.33.5",
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@lhci/cli": "^0.14.0",
    "@playwright/test": "^1.49.0",
    "@tailwindcss/vite": "^4.0.0",
    "@types/node": "^22.10.0",
    "basic-ftp": "^6.2.0",
    "ssh2-sftp-client": "^12.1.1",
    "typescript": "^5.7.2"
  }
}
